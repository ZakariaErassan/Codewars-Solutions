// Very simple, given an integer or a floating-point number, find its opposite.

// First Solution

const opposite_one = number => {return number * -1}

// Second Solution (cleaner)

const opposite_two = number => number * -1;

